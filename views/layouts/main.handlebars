<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <title>WebPro Blog</title>
</head>
<body>
    <header id="header">
        <h1>WePro Blog</h1>
        <div class="navlinks">
            <nav>
                {{#if loggedIn}}
                {{!-- update href for home and dashboard when they are clicked --}}
                <a href="/" >Home</a>
                <a href="/dashboard" >Dashboard</a>
                <a href="#" id="logout">Logout</a>
                {{else}}
                <a href="/login">Login</a>
                {{/if}}
            </nav>
        </div>
    </header>
    <main>
        {{{ body }}}
    </main>
    <footer>
        Web Programming Blog is where we code together!
    </footer>
    {{#if loggedIn}}
    <script src="/javascript/logout.js"></script>
    {{/if}}
</body>
</html>
